{
  "name": "postcss-require-hover-support",
  "version": "0.0.0",
  "description": "PostCSS plugin that wraps hover selectors in a media media that's only valid on devices that support true hover events.",
  "keywords": [
    "postcss",
    "css",
    "postcss-plugin",
    "hover"
  ],
  "author": "Jack Burns <jack@raptiq.com>",
  "license": "MIT",
  "repository": "raptiq/postcss-require-hover-support",
  "bugs": {
    "url": "https://github.com/raptiq/postcss-require-hover-support/issues"
  },
  "homepage": "https://github.com/raptiq/postcss-require-hover-support",
  "dependencies": {
    "postcss": "^6.0.1"
  },
  "devDependencies": {
    "eslint": "^3.19.0",
    "eslint-config-postcss": "^2.0.2",
    "jest": "^20.0.0"
  },
  "scripts": {
    "test": "jest && eslint *.js"
  },
  "eslintConfig": {
    "extends": "eslint-config-postcss/es5",
    "env": {
      "jest": true
    }
  }
}
